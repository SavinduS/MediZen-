name: Payment Service CI

on:
  push:
    branches: ["main"]
    paths:
      - "services/payment-service/**"
  pull_request:
    branches: ["main"]
    paths:
      - "services/payment-service/**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: "services/payment-service/package-lock.json"

      - name: Install dependencies
        run: |
          cd services/payment-service
          npm install

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push Payment Service
        uses: docker/build-push-action@v5
        with:
          context: ./services/payment-service
          file: ./services/payment-service/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/medizen-payment-service:latest
            ${{ secrets.DOCKER_HUB_USERNAME }}/medizen-payment-service:${{ github.sha }}
