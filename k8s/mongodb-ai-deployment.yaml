apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-ai
  template:
    metadata:
      labels:
        app: mongodb-ai
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-ai-service
spec:
  selector:
    app: mongodb-ai
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017